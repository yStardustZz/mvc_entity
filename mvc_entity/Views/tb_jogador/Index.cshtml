@model IEnumerable<mvc_entity.Models.tb_jogador>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nome_jogador)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tb_times.nome_time)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome_jogador)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tb_times.nome_time)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_jogador }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_jogador }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_jogador })
        </td>
    </tr>
}

</table>
